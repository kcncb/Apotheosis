# PID-EventSync（移植 AimMagic 1.0.30 全链路）—— **唯一的瞄准链路**

> 2026-09-15 新增；**2026-09-16 起它是唯一链路**。
> 原先有「经典 PID / EventSync」两档，用 `aim_mode` 选（0 = 经典、1 = 本档）。
> 那个经典档已经**整条删除**：`AimPredict`（`mouse/aim_predict.h`，全局提前量）、
> `aim_mode` 键、`use_prediction_tick` / `prediction_tick_hz` /
> `prediction_tick_max_run`（帧间外推补拍）连同 `tests/aim_predict_test.cpp`
> 一起消失。老配置里残留的 `aim_mode` 被**忽略**（不报错、也不影响任何别的键）。
> ★ 因此本文原先那句"`aim_mode = 0` 时本档一行都不生效、行为逐位不变"**已经失效** ——
> 现在没有"关档"这个状态，本档的代码路径**恒在跑**。
> 回归 `tests/aim_eventsync_test.cpp` §[1] 改为断言的形态是：**三件可关的东西全关**
> （预测系数 0 / 在途换算窗 0 / 自运动 0）时，逐拍输出与教科书 PID **逐位相同**。
>
> ★★★ **2026-09-16 追加更正 —— 本文早期版本有三处已被反编译推翻的说法，阅读时以下面为准**
> （真值表：`docs/aimmagic-ground-truth.md` §0.3 / §0.11 / §7.2 / §7.4 / §8.3–§8.4）：
> 1. **"AM 的 PID 内核/双轴外层没有语料 ⇒ 不能移植"是错的。** `FUN_140056f10` 与
>    `FUN_1400579f0` **都一直有完整伪代码**（`ghidra_out\pid_core.txt`，
>    外层在 **L752-921**）。上一轮只看了 `funcs\` 的文件名清单、没看 `ghidra_out\` 正文
>    就下了"缺失"的结论。**两者现已逐行移植到 `Apotheosis/mouse/aim_pid_am.h`**
>    （API `boss::amPidAxisStep`，回归 `tests/aim_pid_am_test.cpp` **47 项全绿**）。
>    ★★ **但它【尚未接线进生产回路】，这是有意的** —— 生产**仍走 `mouse/aim_pid.h`**
>    （已用实机数据整定：死区 46ms、`inflight_beta=1.6`、`Kp×s_max≲52`）。
>    不接线的三条理由见 `aimmagic-ground-truth.md` **§8.4**：①AM 内核吃的是 **setpoint**
>    而不是误差，外层还要做"按帧比例的增益插值 + 三值取中位数的输出夹取"，依赖 AM 的
>    帧计数器/总帧数/幅度上限三个量，本项目没有对应配置项；②直接替换会一次性废掉现有定标；
>    ③用户要求是"都搬过来"，接不接线是**另一个决定**。**任何说"AM PID 已经是生产件"的说法都是错的。**
> 2. **档位枚举不是"列表下标"，也不是 0x7。** 真实值来自 `FUN_140056f10`/`FUN_1400579f0`
>    的内联立即数（**枚举槽位的比较一律回反汇编读 `CMP <imm>`**，不要信 C 伪代码里的
>    浮点常量 —— Ghidra 把 `CMP 0x8` 渲染成一个**印错了位模式**的 denormal double）：
>    `PID-Free` = **0x8**、`PID-Kalman` = **0xA**、`PID-Adaptive` = **0xC**、
>    `PID-FrameSync` == `PID-EventSync` = **0xD**（**同一个数值**，只靠字符串区分，
>    两者的 **PID 算术完全相同**，都跳 `LAB_140057288`）。见 ground-truth §0.3 / §7.2。
> 3. **D 项混合系数是 `1 − exp(−40π·dt)`（≈0.6476 @ dt=8.3ms），不是线性式。**
>    常数 `0x1401f8030 = −125.66370614359172 = −40π`（**带负号**）；
>    线性式 `1 − 40π·dt` 在 8.3ms 下是 **−0.043**（负数），且两者 `dt→0` 的极限
>    **不同**（0 vs 1）⇒ **定性错误，不是精度问题**。`FUN_1401daf1a` 已确认是 `exp`
>    （IAT thunk）。时间常数 `1/(40π) ≈ 7.96 ms`。见 ground-truth §0.11 / §8.3（N6）。
>    （FrameSync / EventSync / Kalman 共用这一支；Adaptive / Free 走 `dVar18 = 0.3`。）

本文回答三件事：**抄了什么**、**为什么某些东西没抄**、**怎么验证**。
R/E 语料：`Downloads\AimMagic_RE_extracted\AimMagic_RE\v1030\`（只读，未执行）。

---

## 1. 一句话

AimMagic 的「PID-EventSync」= **每推理帧消费一次** + **跟踪器给身份与速度** +
**每轨一份预测状态机** + PID。前两项已经搬进来了；第三项的**结构**搬进来了；
它的**数字**（以及两条需要「每计数像素」的换算链）**没有**搬 —— 见 §4。

```
推理帧 ──► 选靶(既有 selector) ──► 跟踪器(新) ──► 预测状态机(新) ──► PID(既有) ──► 计数
                                       │                  │                │
                                    身份/速度          每轨系数          在途补偿(计数域)
                                    IoU+最近邻       0.1爬 / 0.2落        u -= beta·N/W
```

误差注入点就是 PID 的唯一入口（提前量加在算误差之前），所以下游的尺度调度、
PID、在途补偿、亚量化保护、轨迹曲线与我们自己那套**完全共用、一行没改**。

---

## 2. 抄了哪些（逐项对应语料）

| 移植内容 | 语料来源 | 落点 |
|---|---|---|
| 事件驱动消费：每个新检测只出一拍位移，帧间**不外推补拍** | `FUN_140067000` 主循环（等条件变量，睡到下一帧） | `mouse_thread_loop.cpp`：没有新检测就 `continue`（原来的 `use_prediction_tick` 外推分支已整条删除，与"本档不外推"的语义冲突不存在了） |
| 身份粘滞（trackId 优先 → 再按 IoU 关联 → 新建） | `FUN_1400824e0` 行 680–720 的选靶 + `FUN_140089ac0` 的关联判据 | `AimTracker::offer()` |
| 生命周期 `min_hits` / `max_age` | `FUN_140089ac0` 行 448–450（3 / 5，单位是**帧**） | `AimTrackerParams` |
| 速度**跨帧持有**（带采样窗，而不是逐帧差分） | §4.3 的采样窗速度 | `TrackState::vel_win_*` / `attach()` |
| 每轨预测状态机：系数 0.1/帧 爬升、−0.2/帧 回落、夹在 [0,1] | `FUN_14006e470` 行 133–146（`DAT_1401f461c`=0.1、`DAT_1401f9a08`=0.2、`DAT_1401f4634`=1.0） | `AimTracker::predictionLead()` |
| 尺寸权重 `(maxW−w)/(maxW−minW)`，区间外为 0 | 同上 | `AimTracker::sizeWeight()` |
| 方向翻转阻尼（异号时先压低再低通） | 同上（AM 固定 0.02） | `AimTracker::axisLead()` |
| 「目标真在动」才涨系数（**双条件**：目标动 **且** 自己在动） | 同上 行 133–146（`DAT_1401f9aa0`≈3.5057 是自身速度门） | `predictionLead()` 里的 `rising` |
| ⑤ 在途自身位移补偿：发送环窗口计数求和 ÷ k̂ → 像素，从误差里扣 | `FUN_140067000` 行 1172–1209 | `AimTracker::noteSend()` / `inflightPixels()`，`boss_aim.cpp` 误差合成 |
| ⑤ k̂ 本身（用户手填的常数） | `kalman_counts_per_pixel_x/y`（默认 1.0，范围 [0.001,10000]） | `esync_counts_per_pixel_x/y` |
| ⑥ 自运动项：提前量正比于**自身**瞄准速度 | `FUN_14006e470` 吃 `runtime+0xC04` | `AimTracker::selfMotionLead()`（默认关 + 三夹取） |

---

## 3. 相对"以前那个经典档"的三处结构差别（那三件事已经定案）

> 经典档已经删除，所以本节不再是"两档的差别"，而是**为什么现在这样做**的记录。

### 3.1 结构差别（状态住在哪）

1. **身份从哪来**：旧经典档用 selector 的 `target_generation`；现在用跟踪器
   的 `trackId`。跟踪器的 id 有粘滞性（IoU + 最近邻），所以它换号≈真换目标，于是
   「真换目标」判据不再需要 `跳变 ≥ 25px` 那道闸 —— 那道闸连同
   `last_anchor_`/`has_last_anchor_` 状态**已删除**（`boss_aim.cpp` 里分支写明）。
2. **提前量住在哪**：旧经典档是一份**全局** `AimPredict` 状态（速度来自紧挨瞄点的
   α-β 滤波），那个文件已删除；现在是**每条轨迹**一份系数与平滑状态（速度来自该轨迹的
   采样窗）。换目标不串状态，短暂漏检不清系数。
3. **丢目标时清不清**：旧经典档立即 `reset()` 控制器历史；现在**不清** ——
   轨迹真的死掉（`max_age` 到期、跟踪器手里一条锁定轨迹都没有）或真换目标才复位。
   ★ 依据是 §3.4 的实测：身份变化占 7% 的帧 ⇒ 每秒清 8 次 ⇒ 积分永远攒不起来。

★★ **跟踪器每拍都要推进**（包括没有目标的那几拍）：空观测也要
`beginFrame/endFrame`，否则漏帧计数不增长、超龄轨迹永远不淘汰。
这一点在 `boss_aim.cpp` 的丢目标分支里是显式写明的。

### 3.2 三件可独立开关的事（默认两关一开）

| 件 | 默认 | 打开/生效后改变什么 |
|---|---|---|
| ⑤ 在途换算链（像素域） | **关闭**（窗口 0） | 在途补偿从计数域换成 AM 的像素域做法；此时**自动把 `inflight_beta` 置 0** |
| ⑥ 自运动项 | **关闭**（gain 0） | 提前量多一项正比于自身瞄准速度 |
| ③ 尺寸门限（速度域） | 生效 | 决定预测系数涨不涨；**第二条要求自己在动** |
| ② 预测系数本身 | **为 0**（= 提前量恒 0） | 系数由 `pidf_predict_*` 给，默认 0 ⇒ 即使状态机在跑也不产生提前量 |

★ 这些**全部是共用的**：PID 形式、时间归一化增益、**计数域**在途补偿（默认路径）、
亚量化保护、取整零头、尺度调度、轨迹曲线、扳机 FSM。

★★ **重要**：因为 ③ 的门限第二条要求"自己在动"，而 ⑥ 默认关闭时自身速度仍会被
喂进跟踪器（`setAimVelocity` 每拍都调，用锚点滤波器的速度）—— 所以
**目标在动 + 你在甩枪** 时系数照样能涨起来。⑥ 关掉不等于预测不工作。

---

## 4. k̂ 的更正（2026-09-15）—— 第一项与第二项已移植

> **本节推翻本文件早期版本的一整段结论。** 早期版本写着"在途补偿链与自运动项
> 不移植，因为它们都需要 k̂，而 k̂ 在双机架构下测不出来"。**那个结论一半是错的。**

### 4.1 复查语料的结论：AM 的 k̂ 是**用户手填的常量**，不是测量值

早期版本依据的是 `docs/aimmagic-comparison.md` §6.7，那一节追的是 **`pid_calib`**
（**甩枪档**的自动标定，`FUN_14006bf50`：发已知计数 → 读光标 → 反算 counts-per-pixel）。
那一节本身**没错** —— 它依赖单机架构（程序、光标、游戏同机），在本项目双机架构下
确实不成立。

**但它不适用于 EventSync 档。** 复查 `v1030/ANALYSIS_v1030.md` 后确认，EventSync 档
用的是另一个量：

| 键 | 默认 | 夹取范围 | 见 |
|---|---|---|---|
| `kalman_counts_per_pixel_x` | **1.0** | [0.001, 10000] | ANALYSIS 行 143 / 794 |
| `kalman_counts_per_pixel_y` | **1.0** | [0.001, 10000] | ANALYSIS 行 144 / 795 |

它在代码里的**全部用途**就是一个带下限的除数（`FUN_140067000` 行 1172–1209）：

```c
// 行 1187-1188: 发送环窗口内把计数求和(窗口下界 = now − mouse_effect_delay_ms)
dVar56 += (double)(int)plVar33[1];       // Σ dx (计数)
dVar57 += (double)*(int*)(plVar33+0xc);  // Σ dy (计数)
// 行 1198-1205: 取倒数前先夹下限 DAT_1401f8000(防除零)
dVar50 = max(counts_per_pixel_y, DAT_1401f8000);
dVar51 = max(counts_per_pixel_x, DAT_1401f8000);
// 行 1208-1209: 换算成【像素】, 送进预测器与误差合成
*(double*)(puVar40+0x28) = dVar57 / dVar50;   // y 像素
*(double*)(puVar40+0x20) = dVar56 / dVar51;   // x 像素
```

**没有任何在线估计、观测器或最小二乘。** 配置槽位 `0x90/0x94` → 流水线结构体
`0xea8/0xeb0` → `0x520/0x528`（`FUN_140067000` 行 366–367 逐字段拷贝），就是一个
用户填的常量。

> **默认 1.0 的语义是"1 计数 = 1 像素"，也就是不做换算。**
> AM 把"k̂ 填错"的风险**交给用户承担**，而且它单机架构下用户填的就是自己游戏的真实值。
> 本项目用户**同样可以手填**（他在自己游戏里知道灵敏度），所以这条路是通的 ——
> **不能让程序自动测量**（那才是不成立的部分）。

### 4.2 ⑤ 在途补偿链（已移植）

落点：`mouse/aim_tracker.h` 的 `noteSend()` / `inflightPixels()`，
误差合成在 `mouse/boss_aim.cpp`。

| AM | 本实现 | 说明 |
|---|---|---|
| 时间戳窗口（`now − mouse_effect_delay_ms`） | **定长环形缓冲** | 与 `aim_pid.h` 计数域补偿同一套做法：可复现、不依赖时钟抖动 |
| 窗口内计数**求和** | 窗口内计数求和 | 同 |
| `÷ counts_per_pixel` | `÷ (k̂ 或 0.001)` | 同，含 AM 的防除零夹取 |
| 直接送进误差合成 | `÷ 窗口拍数` 再送 | ★ **必要的偏差**，见下 |

> ★ **为什么必须除以窗口拍数**：AM 求的是"一个延迟窗口内发出去的总量"，
> 而我们要扣的是"本拍该扣多少"。不除会导致同一批计数被重复扣 W 次 ——
> 这正是本项目在途补偿的实测教训（1000fps 尾段 8.30px，见 CLAUDE.md 要点②）。
> 除完之后与 `u -= beta·N/W` **完全同构**，只是量纲从计数换成了像素。

★★ **与计数域补偿互斥（代码强制，判据是"窗口是否为 0"，不再是"档位"）**：
两者是同一个 Smith 预测器的两种做法。
`AimEngine::tick()` 里 `am_inflight = (in.esync.inflight_window_s > 0.0)`，
为真时**自动把 `inflight_beta` 置 0**，同时开会把同一批在途指令扣两次
= 过补偿 = **正反馈发散**。

默认**关闭**（`esync_inflight_window_ms = 0`）：打开它等于把在途补偿从"不需要 k̂ 的
计数域"换成"需要用户填对 k̂ 的像素域"。已验证的老路子是前者。
★★ **反过来不要搞错**：窗口 = 0 时那条像素域链根本没在跑（`inflightPixels()` 恒 0），
此时计数域那项是**唯一**的在途补偿，**绝不能顺手把它也置 0** ——
实测 `beta = 0` 不是"少个优化"，60fps 直接发散（见 CLAUDE.md 的扫描表：
0.0→299.6px、1.6→0.294px、2.0→9.53px）。

### 4.3 ⑥ 自运动项（已移植，但默认关闭 + 三道夹取）

AM 的预测偏移里有一项吃 `runtime+0xC04`（自身瞄准速度）。这一类自运动补偿正是本项目
在 `1a5a792` 删掉的 `predictive_controller`：它把**像素/计数标定增益放在反馈回路内部**，
标定不准就在锚点附近每拍反向极限环（不是抖动，是持续来回摆）。

**移植时保留 AM 的形状，但加三道夹取：**

1. `esync_self_motion_gain` **默认 0**（不生效）；
2. 幅度硬夹取 **±1.0**（`kSelfMotionMaxGain`）；
3. 输出仍走**同一个硬像素上限**，且**两项相加之后**再夹一次（否则同时开两项可突破上限）。

★ 用法上必须说清楚：AM 的预测系数有**第二个条件**（见 §4.4），
**自己没在动时系数只落不涨**。所以不开这一项时，即使目标在动，系数也涨不起来。

### 4.4 ③ 尺寸门限（已改成**速度域**）

AM 的判据是（`FUN_14006e470` 行 119–146，一条 if/else）：

```
条件(涨) =  位移 > max(30, 0.75×size) × (系数×1.5 + 1)     ← ① 目标动得够快
         || |自身瞄准速度| <= fVar9(≈3.5057 px/帧)          ← ② 自己没在动
```

★★ **注意 ② 是"或"，而且它是"落"那一支的条件** —— 原文：
`if (① <= 位移 || ② <= 自身速度) { 系数 -= 0.2 } else { 系数 += 0.1 }`。
也就是说：**只有"目标位移够大 且 自己也在动"时系数才涨。**

原文 ① 吃的是**本帧位移**（像素）。照搬到本项目**不能工作**：它隐含假设"相邻推理帧
之间的位移"与框宽同量级，而 120fps 下 300px/s 的目标每帧只走 2.5px，60px 宽的框门限
却是 45~112px —— 差 20~45 倍，**门限恒不成立，系数永远爬不上去，预测被静默关掉**
（比不移植更糟：用户会以为"预测坏了"，而代码里没有任何报错）。

**改法：只换量纲，不换常数。** 把"每帧位移"换成"每秒位移"（÷ 帧间隔）：

```
v > max(30px, 0.75×框宽) × (1 + 1.5×系数) ÷ 帧间隔
自身速度 > 3.5057 ÷ 帧间隔
```

三个常数（30 / 0.75 / 1.5）与自身的 3.5057 **一个都没改**，只是把帧率从判据里显式
拿出来 —— 换任何帧率都还原 AM 的原意。

> ⚠️ **2026-09-16 复核更正（本文这一节是"速度域改造版"，不是 AM 原文）**：
> 逐字移植时发现上表有三个常数/形状与真值不符，**真值以
> `docs/aimmagic-ground-truth.md` §5.2 / §8 第 1–3 条 / §8.3（N1、N3）为准**：
> - 门限常数是 **0.8**（不是 0.75）；自身速度门是 **10.0**（不是 3.5057）——
>   ground-truth §8 第 2、3 条。
> - 两轴的"尺寸"是**交叉**的：**X 轴吃框高、Y 轴吃框宽**（原文如此）—— §8.3 的 N1。
>   后果：高瘦框在 X 上容易触发、在 Y 上几乎不可能。
> - **涨的完整条件是"与"**：`|框逐帧位移| > max(30, 0.8×尺寸)×(1+1.5k)`
>   **且** `|自身逐帧位移| > 10px`，**两个都是必要条件** ⇒ **目标静止时系数恒 0**。
> - 位移量纲是 `vel × dt_s`（**秒**），不是 `vel × dt_ms`（§8.3 的 N3）。
> - 输出还要过一道**重低通**：正常混合 **0.05**、方向翻转 **0.02**（§8.3 的 N2）。
> ★ 本节上面那段"÷ 帧间隔"的改造**仍然有效**（它是本项目当时的有意偏差），
> 但**逐字移植后的 `mouse/aim_tracker.h` 用的是 AM 原式的每帧位移**，两者不要混读。

> ★ 两个条件都接上之后，这个 FSM 的手感是：**你在甩枪、目标也在动 → 加提前量；
> 目标停下或你停下 → 立刻收手**。这也解释了 AM 为什么叫它"自摇领先量"。

### 4.5 仍未移植 / 未接线项

- **★ AM 的 PID 内核本身（`FUN_140056f10` + `FUN_1400579f0`）—— 已移植，但【未接线】。**
  这条**替换掉本文早期版本那句"AM 的 PID 内核缺语料所以没法移植"**（那是错的：
  外层完整伪代码一直在 `ghidra_out\pid_core.txt` **L752-921**）。
  现状：逐行移植在 **`Apotheosis/mouse/aim_pid_am.h`**（API `boss::amPidAxisStep`），
  回归 **`tests/aim_pid_am_test.cpp`（47 项全绿）**；
  **生产回路仍走 `mouse/aim_pid.h`，`aim_pid_am.h` 一行都没接进去。**
  不接线的三条理由与接线前必须满足的三条前提，见
  `docs/aimmagic-ground-truth.md` **§7.4 与 §8.4**。**不要对外说"AM PID 已上线"。**
- **轴锁**（`FUN_140057df0`）：输出侧整形，本项目已有 `mouse/aim_path.h` 承担同类职责。
- **`aim_follow_x/y` 输出增益**（`FUN_140067000` 行 1269）：与 `pidf_kp_*` 重复，
  本项目用 PID 增益表达。
- **`pid_calib` 自动标定**：见 §4.1，那条路依赖单机架构，**仍然不成立**。
  本档的 k̂ 由用户手填，不做自动测量。
  ★ 注意区分：说"自动标定不成立"**不等于**"AM 没有 k̂"—— AM **真的拿 k̂ 当除数**
  （默认 1.0 ⇒ 恒等，这才是它"看着像没有"的原因），见
  `docs/aimmagic-ground-truth.md` §6 / §8 第 15–16 条。
- **选靶打分**：AM **有**独立的选靶打分器 **`FUN_140088ab0`**（外壳 `FUN_140088a30`，
  调用点 `FUN_1400824e0`，读 `size_scoring_weight` / `distance_scoring_weight`）
  —— 本项目**没有**移植它；而且它的**打分公式至今未破解**
  （`center_scoring_weight` 是 0 引用的死键），**在溯源清楚之前不许照抄**。
  见 `docs/aimmagic-ground-truth.md` §0.15 / §9.10–§9.11。

---

## 5. 安全约束（本档必须满足，全部有回归）

| 约束 | 落点 | 断言 |
|---|---|---|
| 提前量**硬像素上限** | `pidf_predict_max_px`（默认 12，绝对天花板 64） | `aim_tracker_test` §[5]、`aim_eventsync_test` §[2] |
| 速度**噪声门** | `pidf_predict_vel_floor`（默认 60px/s） | `aim_tracker_test` §[5] |
| 关预测时**逐位恒等** | 系数 0 ⇒ lead 恒 (0,0) | `aim_eventsync_test` §[1] |
| 闭环**不发散** | 系数拉到 ±0.2 边界仍必须有界 | `aim_eventsync_test` §[2] |
| **不叠加两层外推** | 帧间预测拍整条删除（没有新检测就 `continue`），结构上不可能叠 | `mouse_thread_loop.cpp`（原分支已删） |
| 坏配置**安全回落** | 已删除的 `aim_mode` 残留键被忽略（不报错、不影响任何键） | `config_migration_test` §[8] |

---

## 6. 配置槽位

全部是**新增键**，所以 **`pidf_mapping_version` 不推进**（版本号是"旧槽位语义变更"
的迁移机制，这里没有任何旧槽位被改语义）。老配置缺这些键 ⇒ 取结构体默认 ⇒
两关一开（预测系数 0 / 窗口 0 / 自运动 0）⇒ 控制器退化成教科书 PID。
★ 2026-09-16 删掉 `aim_mode`、`use_prediction_tick`、`prediction_tick_hz`、
`prediction_tick_max_run` 之后**仍然不推进版本号** —— 删掉的键不参与迁移，
老文件里残留它们既不会报错也不会改变任何一个存活槽位。
`config_migration_test` §[8] 就是钉这一条（塞进 `aim_mode = 0/1/7/-3`，断言既不报错、
也不影响 `esync_min_hits` 之类真实存在的键）。

| 键 | 默认 | 含义 |
|---|---|---|
| ~~`aim_mode`~~ | — | **已删除（2026-09-16）**。本档是唯一链路，不再需要档位开关；残留键被忽略 |
| `esync_min_hits` | 3 | 连续命中多少帧算"确认轨迹"（AM 原值） |
| `esync_max_age` | 5 | 漏帧多少帧后删除轨迹 = 滑行窗口（AM 原值） |
| `esync_assoc_radius_px` | 80 | 关联最近邻门限（框心距离） |
| `esync_assoc_iou` | 0.20 | 与锁定轨迹的 IoU 门限（0 = 只靠最近邻） |
| `esync_vel_window_ms` | 100 | 速度采样窗；0 = 逐帧重算（不推荐） |
| `esync_counts_per_pixel_x` | **1.0** | ⑤ 每计数像素（AM 的 `kalman_counts_per_pixel_x`）。1.0 = 不换算 |
| `esync_counts_per_pixel_y` | **1.0** | ⑤ 同上（Y 轴） |
| `esync_inflight_window_ms` | **0** | ⑤ 在途换算窗；**0 = 整条链关闭**。上限硬夹 46ms（死区） |
| `esync_inflight_beta` | **1.0** | ⑤ 在途换算强度（AM 无额外增益，= 1.0）。夹 [0, 4] |
| `esync_self_motion_gain` | **0.0** | ⑥ 自运动补偿增益；**0 = 关闭**。夹 ±1.0 |

预测强度、尺寸区间、硬上限、噪声门用同一组槽位（`pidf_predict_*`），消费点是
`AimTracker::predictionLead()`（**唯一的**提前量来源）——
它原来是"两档共用一份预测强度"留下的设计，现在只有一条链路，于是它就是
**用户调提前量强度的唯一旋钮**（预测系数默认 0 ⇒ 提前量恒 0）。

★★ **`esync_inflight_window_ms > 0` 时会自动关掉 `pidf_inflight_beta`**（代码强制，
见 `boss_aim.cpp`）。两者是同一个 Smith 预测器的两种做法，同开 = 扣两次 = 发散。

UI：`qt_ui/pages/HotkeyPage.cpp` 的「瞄准档位（PID-EventSync）」一段。

---

## 7. 验证

```powershell
ctest --test-dir build/logic-tests -C Release
```

| 测试 | 覆盖 |
|---|---|
| `tests/aim_tracker_test.cpp`（**347 项**） | 身份粘滞 / 生命周期 / **锁定轨迹死亡后的身份接管** / 速度窗收敛与跨帧持有 / 系数 FSM（含步长 0.1、0.2）/ **双条件门（自己不动时系数不许涨）** / 硬上限与噪声门 / 关预测恒等 / 确定性 / 异常输入 / 参数夹取 / **在途换算链（k̂=1/2/0.5 的比例、窗口 0 = 关闭、死区夹取、清账本）** / **自运动项（默认关 + 硬上限 + 非法输入）** / **k̂-死区常数一致性** / **在途补偿二选一判据（§[10.5]，含 NaN/负窗口与"拍数>0"一致性）** |
| `tests/aim_eventsync_test.cpp`（**30 项**） | **三件可关的件全关时与教科书 PID 逐位一致** + 反向验证（开自运动 / 开在途换算必须不同）/ 提前量有界不发散 / 丢帧时积分不被清零 / 身份只建一次 / 在途补偿与档位无关（现在恒为计数域那套） |
| `tests/config_migration_test.cpp` §[7][8] | 新增键不推进版本、**新键默认值（k̂=1.0 / 窗口 0 / 自运动 0）**、参数夹取（**含 k̂=0 回落、窗口夹到 46ms**）、**已删除的 `aim_mode` 残留键被忽略** |
| **`tests/aim_pid_am_test.cpp`（47 项）** | **AM PID 内核逐字移植**（`FUN_140056f10` + `FUN_1400579f0`）：档位枚举 `0x8/0xA/0xC/0xD`、D 混合 `1−exp(−40π·dt)`、第 1 帧早退只跳 D/F、`+0xFD` 只在 Adaptive 档是闸门等（N5–N9，见 `aimmagic-ground-truth.md` §8.3）。★★ **它验的是 `mouse/aim_pid_am.h` 这个独立件，不是生产回路** —— 生产仍走 `mouse/aim_pid.h`（未接线，见 §4.5 与 ground-truth §8.4）。 |

★ **逻辑测试总数现为 24 个（原 23），24/24 全绿**（`aim_pid_am_test` 是本轮新增的那个）。

**没有验证到的**（必须说清）：

- **实机手感**。逻辑测试用假游戏（世界速度 + 46ms 延迟 + 框心整数量化），
  它证明的是"有界、收敛、降级等价"，**不证明**这套参数在实机上比纯反馈更好。
- **`boss_aim.cpp` 的接线本身**。闭环测试手搭的是同一条算式，不含 selector；
  接线正确性只由主程序编译通过保证。
- **⑤ 在途换算链的实机效果 —— 而且它依赖用户填对 k̂。** 逻辑测试证明了"比例对、
  有界、窗口能被夹住、关闭时逐位一致"，但 **k̂ 填错的后果没有被测试覆盖** ——
  因为那取决于用户的真实灵敏度，程序无从判断。用法上建议：**先只用计数域那项**
  （那是已验证的路子），想完整复刻 AM 形态时再打开 ⑤ 并把 k̂ 填对。
- **删除动作本身没有实机验证。** 2026-09-16 的"删掉经典档"只做到
  "逻辑回归全绿 + 结构检查通过"; `boss_aim.cpp` / `mouse_thread_loop.cpp` 在本机
  (macOS) **没有被编译过**（它们需要 OpenCV + `ava_exact`），实机手感与编译期正确性
  都还没验。`docs/eventsync-mode.md` 的读者请以"代码 + 回归通过"为准，不要当成
  实机结论。

---

## 8. 写这个跟踪器时踩的坑（都是回归抓出来的）

★★★ **「关联不上就接管锁定」在一帧多候选时会让【最后喂进来的那个】赢。**
`offer()` 的三级关联里，③ 新建轨迹必须顺便接管锁定（否则两个不同目标会串成
同一个 id，"换目标复位"永不触发）。但接口允许一帧喂多个候选，于是每个"关联不上"
的候选都去抢锁 ⇒ 本帧**最后**一个候选赢。而调用方的意图恰恰是"先喂的优先级高"。

实测症状（`aim_tracker_test` §[2b]）：两个远隔目标 A(400,300) / B(900,300) 交替出现，
先喂 A 后喂 B，结果锁定一直停在 **B** 上，A 死了以后 id 完全没变 —— 也就是
"换目标"这件事被静默吞掉了。

修法：加 `lock_taken_this_frame_`，`beginFrame()` 清零，三个分支都尊重它
（一帧只认第一次定下的身份）。修完 A→B 的接管 id 从 `2→2`（错）变成 `1→2`（对）。

★★ **`min_hits` 绝不能挡锁定。** 最初的 `locked()` 要求 `confirmed`，等价于
"目标刚露头 3 帧内完全锁不上"（`aimmagic-port-spec.md` §2.3 实测过这个坑）。
现在 `locked()` 不看 confirmed；`min_hits` 只用于遥测标记与"锁定轨迹死后能否
接管"。

★★ **速度不能逐帧差分。** 8.3ms 的相邻两框心差分把量化噪声放大成几百 px/s 的
假速度（静止目标实测噪声 p99=46px/s）。所以速度必须走采样窗并**跨帧持有**。
★ 顺带一个语义细节：噪声门判据必须用**严格大于**（`speed > floor`），
否则门限填 0 时"任何速度都算动"，目标停下来了系数还挂在 1，提前量永不收手
（这条也是回归抓出来的）。

★★ **系数 FSM 只在 `predictionLead()` 里推进。** 调用方每拍必须调它一次，
**且只调一次** —— 调两次等于一拍走两步（爬升会看到 0.2/帧）。
`boss_aim.cpp` 的接线正是每拍一次。

---

## 9. 2026-09-15 第二轮（移植 ⑤⑥ + ③ 改速度域）踩的坑

★★★ **"这个量测不出来"必须先问"它到底是测的还是填的"。**
我第一轮把 `pid_calib`（甩枪档的**自动标定**，确实依赖单机架构、确实不成立）的结论
直接套到了 EventSync 档，于是判定"k̂ 不可得 ⇒ ⑤⑥ 不能抄"。**这是错的** ——
EventSync 档用的是 `kalman_counts_per_pixel_x/y`，一个**用户填的常量**。
两个量同名（都是"每计数像素"）、来源完全不同（一个在线反算、一个手填），
混为一谈就得出一个过于保守的结论，白白砍掉两项功能。

> **教训**：写"某功能不可实现"的结论时，必须指名道姓写出**是哪个函数/哪个槽位**
> 让它不可实现。如果只能写出"这个量测不出来"而说不出"AM 在**这一档**是怎么拿到的"，
> 那个结论就还没查到底。

★★★ **双条件门里藏着一个"或"，我漏了整整一轮。**
`FUN_14006e470` 行 133–135 的完整条件是
`if (门限 <= 目标位移 || |自身速度| <= 3.5px/帧) { 落 } else { 涨 }`。
我第一轮只抄了第一条，于是把它当成"目标在动就涨"。

**漏掉第二条的后果是双向的**：
- 在**本档**：预测会在"自己在甩枪但目标静止"时错误地涨起来（多发提前量）；
- 反过来接自运动项时：不喂自身速度 ⇒ 系数**永远不涨** ⇒ 我们会以为"预测坏了"。
  实测就是这样：FSM 用例里 `vel=(0,0) valid=0`、系数恒 0，查了半天才发现
  ① 目标每帧走 166px 超过 80px 关联门限（每帧新建轨迹、速度窗攒不满）
  ② 自身速度压根没接。

> **教训**：反汇编里的复合条件要看**完整的那一条 if**，包括 `||` 后面那一半；
> 只看第一个比较就下结论，会把一个"与"误读成充分条件。

★★ **`predictionLead()` 里的早退分支会连带跳过新加的功能。**
它原本有两处早退：`!t->confirmed`（错，见上）和 `size_w <= 0`（形态外）。
自运动项加进去之后，第二处早退会把**与框大小无关**的自运动补偿也一起吃掉。
修法：形态外分支里仍然计算自运动项。

> **教训**：往一个"分层计算 + 早退"的函数里加新项时，要检查**每一处 `return`
> 是否应该带上新项**；这类 bug 的测试症状是"功能在某个参数区间静默失效"。

★★ **`min_hits`（confirmed）不该挡预测，和它不该挡锁定是同一条理由。**
`predictionLead()` 原本 `!t->confirmed` 直接返回 false ⇒ 目标刚露头的头 3 帧
预测不工作。AM 的预测状态机**没有 confirmed 这个概念**（它每帧都推进系数）。
已在同一轮一并修掉。

★ **测试用例本身也会"跟丢"。** 用 20000px/s 的目标测 FSM 时，每帧位移 166px
超过默认 80px 关联门限 ⇒ 每帧新建轨迹 ⇒ 速度窗永远攒不满 ⇒ `vel_valid` 恒为 0。
这不是代码 bug，是"目标快到跟丢"的物理必然 —— 但**测试必须先把这个前提写清楚**
（放宽 `assoc_radius_px`），否则测的是"跟丢了"而不是"门限不成立"。

★ **"逐位一致"的前提随功能增加而变化。** 第一轮 EventSync 只有预测一件新东西，
所以"关预测 ⇒ 逐位一致"。现在有三件，前提必须改成**三件全关**，
而且每一件都要有**反向验证**（开着必须不同），否则那条一致性断言会随功能增长
慢慢变成空转。本轮给 ⑤⑥ 各补了一条反向验证。

---

## 9. 2026-09-16：删掉经典档（这次改动的完整记录）

用户原话："当前有两套控制器层面" → "删一下 就只保留 eventsync 这套" →
"eventsync 需要 aimpredict 吗 不需要就删 保留 AM 的"。

**删除清单**（都已落地）：

| 删掉的东西 | 原来干什么 | 为什么可以删 |
|---|---|---|
| `aim_mode`（config 键 + UI 下拉 + 加载/保存/夹取） | 选经典档 / 本档 | 只剩一条链路，开关没有意义。残留键被忽略，不写迁移 |
| `Apotheosis/mouse/aim_predict.h` + `tests/aim_predict_test.cpp` | **全局**提前量（系数 × 尺寸权重 × 速度） | 只有经典档接线到它；本档的提前量在 `AimTracker::predictionLead()`，且是**每轨**的（换目标不串味、漏检不清零）。**本档不需要它** |
| `use_prediction_tick` / `prediction_tick_hz` / `prediction_tick_max_run` + `mouse_thread_loop.cpp` 里那一整段外推补拍机器（节流、`last_input`/`last_pivot`/`last_send_capture_ns` 缓存） | 检测间歇期用预测分支继续出拍，把控制节拍与检测节拍解耦 | 本档的语义是**每推理帧一拍、帧间不外推**（AM 另一个档才有帧间插值）。留着就是三个**静默无效**的键 |
| `boss_aim.cpp` 的 `last_anchor_` / `has_last_anchor_` / 「瞄点跳 ≥ 25px」判据 | 经典档判"真换目标" | 身份改由跟踪器给：**它换号就是真换目标**，粘滞性替掉了那道闸 |
| 旧观测器相关的一整段注释（`AnchorObserver`、在线 k̂ 估计、前馈） | 描述早已删除的管线 | 注释描述的东西在代码里不存在，会误导读者 |

**保留并明确的决定 —— 在途补偿**（用户："保留 AM 的"）：

`AimTracker::inflightPixels()`（AM 的发送环，⑤）保留；`AimEngine` 里原本
"本档恒把 `inflight_beta` 置 0" 改成 **`am_inflight = (窗口 > 0)` 的二选一**。
★★ **这次差点犯的错**：把"档位判断"顺手改成"恒为真"，就会**无条件**把计数域的
`inflight_beta` 清零。而窗口默认为 0（AM 那条链根本没在跑），于是默认配置下
**在途补偿整条消失** —— 那是"拆掉主刹车"，不是"删掉一个可选优化"
（实测 `beta = 0` 时 60fps 尾段 299.6px 且会发散，`1.6` 时 0.294px）。
**教训：删掉一个档位时，不要把档位判断替换成常量；要把判据换成"那条替代链
到底有没有在跑"。**

**这次没有做的事**（有意）：

- **不推进 `pidf_mapping_version`**。它是"旧槽位语义变更"的迁移机制，
  而这里只有键的增加与删除，没有任何存活槽位被改语义。
- **不写 `aim_mode` 的迁移/报错**。留着它没有"回到经典档"的去处，
  报错只会让老配置文件变成噪声。
- **不实机验证**。本机是 macOS，`boss_aim.cpp` / `mouse_thread_loop.cpp` 连编译都没跑过
  （需要 OpenCV + `ava_exact`）。所以这次交付的性质是"逻辑回归全绿 + 结构检查通过"，
  **不是**"实机验证过"。
