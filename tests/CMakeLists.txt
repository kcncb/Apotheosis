find_package(Threads REQUIRED)
set(APP_SOURCE_ROOT "${CMAKE_CURRENT_LIST_DIR}/../Apotheosis")
function(add_logic_test target source)
    add_executable(${target} "${source}")
    target_include_directories(${target} PRIVATE "${APP_SOURCE_ROOT}")
    target_compile_features(${target} PRIVATE cxx_std_20)
    target_link_libraries(${target} PRIVATE Threads::Threads)
    set_target_properties(${target} PROPERTIES AUTOMOC OFF)
    if(MSVC)
        target_compile_options(${target} PRIVATE /utf-8 /Zc:char8_t-)
    else()
        target_compile_options(${target} PRIVATE -fno-char8_t)
    endif()
    add_test(NAME ${target} COMMAND ${target})
    set_tests_properties(${target} PROPERTIES TIMEOUT 20)
endfunction()
add_logic_test(latency_probe_test "${APP_SOURCE_ROOT}/runtime/tests/latency_probe_test.cpp")
add_logic_test(capture_card_caps_test "${APP_SOURCE_ROOT}/capture/tests/capture_card_caps_test.cpp")
add_logic_test(device_frame_age_test "${CMAKE_CURRENT_LIST_DIR}/device_frame_age_test.cpp")
add_logic_test(interruptible_slot_test "${CMAKE_CURRENT_LIST_DIR}/interruptible_slot_test.cpp")
add_logic_test(gpu_ready_event_test "${CMAKE_CURRENT_LIST_DIR}/gpu_ready_event_test.cpp")
# This test verifies event ownership with an instrumented CUDA API, not GPU work.
target_include_directories(gpu_ready_event_test BEFORE PRIVATE "${CMAKE_CURRENT_LIST_DIR}/stubs")
add_logic_test(raw_frame_layout_test "${CMAKE_CURRENT_LIST_DIR}/raw_frame_layout_test.cpp")
add_logic_test(latest_move_slot_test "${CMAKE_CURRENT_LIST_DIR}/latest_move_slot_test.cpp")
add_logic_test(predictive_controller_test "${CMAKE_CURRENT_LIST_DIR}/predictive_controller_test.cpp")
target_sources(predictive_controller_test PRIVATE "${APP_SOURCE_ROOT}/mouse/control/predictive_controller.cpp")
add_logic_test(aim_engine_test "${APP_SOURCE_ROOT}/mouse/tests/ava_chain_regression_test.cpp")
file(GLOB ENGINE_LOGIC_SOURCES "${APP_SOURCE_ROOT}/mouse/ava_exact/*.cpp")
target_sources(aim_engine_test PRIVATE ${ENGINE_LOGIC_SOURCES}
    "${APP_SOURCE_ROOT}/mouse/boss_aim.cpp" "${APP_SOURCE_ROOT}/mouse/control/predictive_controller.cpp")
target_include_directories(aim_engine_test BEFORE PRIVATE "${CMAKE_CURRENT_LIST_DIR}/geometry_stubs")
add_logic_test(continuous_tracking_test "${CMAKE_CURRENT_LIST_DIR}/continuous_tracking_test.cpp")
target_sources(continuous_tracking_test PRIVATE "${APP_SOURCE_ROOT}/mouse/control/predictive_controller.cpp")
set_tests_properties(continuous_tracking_test PROPERTIES TIMEOUT 60)
add_executable(continuous_tracking_bench EXCLUDE_FROM_ALL continuous_tracking_bench.cpp
    "${APP_SOURCE_ROOT}/mouse/control/predictive_controller.cpp")
add_executable(continuous_tracking_baseline EXCLUDE_FROM_ALL continuous_tracking_bench.cpp
    reference/continuous_before/mouse/control/predictive_controller.cpp)
foreach(target continuous_tracking_bench continuous_tracking_baseline)
    target_compile_features(${target} PRIVATE cxx_std_20)
    target_link_libraries(${target} PRIVATE Threads::Threads)
    set_target_properties(${target} PROPERTIES AUTOMOC OFF)
endforeach()
target_include_directories(continuous_tracking_bench PRIVATE "${APP_SOURCE_ROOT}")
target_include_directories(continuous_tracking_baseline PRIVATE "${CMAKE_CURRENT_LIST_DIR}/reference/continuous_before")
